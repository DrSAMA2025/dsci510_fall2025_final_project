# --- API Credentials for Project ---
# These variables must be set in a separate .env file
# for the project to run the data collection modules.

# Reddit API (PRAW)
REDDIT_CLIENT_ID=""
REDDIT_CLIENT_SECRET=""
REDDIT_USER_AGENT="dsci510_final_project_v1.0"

# Note: Google Drive data links are configured in config.py
# for pre-collected dataset access during testing.